Package to ilustrate how to create a RR robot using URDF from Lecture 10.

    $ cd /workspaces/COMP3766
    $ colcon build --symlink-install
    $ ros2 launch lec10_urdf display.launch.py 

Open the browser at `localhost:6080`